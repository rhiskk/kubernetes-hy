apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ ingress_name }}
spec:
  rules:
  - http:
      paths:
      - path: /{{ dummysite_name }}
        pathType: Prefix
        backend:
          service:
            name: {{ service_name }}
            port:
              number: 80